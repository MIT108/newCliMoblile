import { StyleSheet, Text, View } from 'react-native'
import React, { ReactNode } from 'react'

type Props = {
    children: ReactNode
}

const PrimaryScreen = ({children}: Props) => {
  return (
    <View>
    </View>
  )
}

export default PrimaryScreen

const styles = StyleSheet.create({})